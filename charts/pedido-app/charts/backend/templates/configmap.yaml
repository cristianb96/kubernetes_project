apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-config
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  DB_HOST: "{{ .Values.env.db.host }}"
  DB_PORT: "{{ .Values.env.db.port }}"
  DB_NAME: "{{ .Values.env.db.database }}"
